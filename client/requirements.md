## Packages
@apollo/client | GraphQL client for SkyWalking API
graphql | GraphQL parser and types
recharts | Data visualization for metrics and traces
zustand | Global state management for time ranges
date-fns | Date formatting for charts and logs

## Notes
The backend proxies GraphQL requests to /api/graphql
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["Inter", "sans-serif"],
  mono: ["JetBrains Mono", "monospace"],
}
