# Copy to .env on Oracle VM: cp .env.example .env

# Aiven OpenSearch (persistent storage for SkyWalking)
OPENSEARCH_HOST=skyobserve-es-travobuds-stg.f.aivencloud.com:16148
OPENSEARCH_USER=avnadmin
OPENSEARCH_PASSWORD=your_password_here

# Use elasticsearch (Aiven OpenSearch) or h2 for in-memory only
SW_STORAGE=elasticsearch
